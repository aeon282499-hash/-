# 設計案: デイトレ用「フィボ押し目反発」モジュール（2026-09-17・実装前レビュー用）

依頼: 本人指示書（9:00〜9:30の初動→高値確定→フィボ→押し目エントリー通知・記録・再生）。
既存のスイング（25MA初押し・-5%損切り等）には触れない。発注はしない。通知と記録のみ。

---

## 0. 先に言っておくこと（既存の検証結果）

同じ土俵の検証を3本済ませていて、**買いのフィボ押し目は5分足59日（1,161銘柄）で全条件PF<1**でした
（当日スイングのフィボ押し目買い PF0.69〜0.85／ランダム同時刻買い -0.1%/件／出口は「引け」が最良で、利1損1やMA割れの損切りはPF0.6〜0.8に悪化）。
`_bt_fibo_5m_ma25_0916.py` → `_log_fibo_5m_ma25_0916.txt`。

今回の指示書で**未検証の新規性**は次の3点だけです。ここが効くかどうかが勝負なので、通知を作る前に再生モードで59日ぶん回して基準線（ランダム同時刻買い）を超えるかを確認する工程を設計に入れます。
1. 「その日最初の大きな波」に限定（初動ランクA/B/C・出来高3倍）
2. 重なりスコア（VWAP/中期MA/前日高値終値/価格帯出来高/キリ番/寄り足高値）
3. 78.6%割れ or -3%の早い方の損切り＋+3%半分利確（従来の検証に無い出口）

---

## 1. データ: 何が取れて何が取れないか

| 必要な物 | 立花e支店API | 実態と代替 |
|---|---|---|
| 5分足・1分足（履歴） | **無い**（`tachibana/client.py` は日足 `price_history()` と時価 `market_price()` のみ） | **自前で積み上げる**。稼働中の `tachibana_live_flow.py` が流動上位約2,500銘柄の 現在値/始値/高値/安値/出来高/代金/VWAP/前日終値 を約1分で一巡している（毎分上書きで履歴なし）→ **毎分の断面を日別ファイルに追記保存**し、そこから1分足→5分足を作る |
| 板 | あり `board()`（売買10本） | 候補銘柄だけ10秒間隔で取る（`tachibana_board_recorder.py` と同じ作法・p_noはファイルロックで共有済み） |
| VWAP | あり `pVWAP` | そのまま |
| 前日同時間帯の出来高 | 無い | 上の毎分保存が貯まれば前日ファイルから出せる。**貯まるまでの代替**＝LiveFlowが既に持つ「20日平均代金の時刻進捗比（flow）」で3倍判定 |
| 価格帯別出来高 | 無い | 毎分断面の Δ出来高を現在値の価格帯に積む近似（当日分）＋前日は5分足から |
| 前日高値・前日終値 | あり（日足・`pPRP`） | そのまま |
| 再生用の過去5分足 | 無い | **`_intraday_cache_5m.pkl`（yfinance 5分足・1,161銘柄・2026-06-24〜09-16・59営業日）を使う**。罠: 足ラベルは開始時刻、9:00足が出来高0の気配足になる日がある、60日ローリングで古い分が消える（月1で再取得） |

1分断面から作る5分足の精度: 始値・終値・出来高は正確（Δ出来高）。高値・安値は「1分ごとの現在値の最大最小」なので足の中の瞬間高値を取りこぼす。ただし**当日高値/安値は `pDHP/pDLP` で毎分正確に取れる**ので「高値更新」「起点割れ」の判定は正確にできる。候補銘柄は10秒ポーリングに切り替えて精度を上げる。

例示3銘柄の手元データ確認（yfinance 5分足）:
- 石川製作所 6208: 前日終値1,842→寄り1,977（GU+7.3%）→高値1,988が最初の5分→9:45に78.6%線(1,873)割れ ＝ **指示書どおり「見送り」になる**
- Zenmu 338A: 前日終値3,110→9:10高値3,290（+5.8%・ランクA）→**1回目の押し安値3,075は起点(前日終値)を割る**＝現行ルールでは「破棄」。あなたの板の形と違う可能性 → 再生モードで最初に突き合わせる項目
- 共栄タンカー 9130: **キャッシュの母集団に無い**（`_intraday_targets.json` に足して再取得、または当日以降の毎分保存で対応）

---

## 2. どこに何を作るか（既存には手を入れない）

```
fibo_daytrade.py            ← 新規・自己完結（定数は冒頭に集約＝既存の流儀）
  ├ bars.py 相当   : 1分断面→1分足→5分足、yfinance 5分足の読み込み（再生用）
  ├ engine         : 起点/初動ランク/高値確定/フィボ/エントリー条件/重なりスコア/決済ルール/見送り理由/寄り天反発
  ├ risk           : 2連敗で当日停止・負け後30分クールダウン・HALF_LOT_MODE(最初の10トレード半ロット)  ※既存に実装なし＝新規
  ├ notify         : Discord（DISCORD_WEBHOOK_FIBO_URL・未設定なら無言スキップ＝shodo_notify.pyの作法）
  ├ record         : fibo_daytrade_log.csv（指示書9の列）＋ fibo_signals/YYYY-MM-DD.json（全判定・見送り含む）
  └ modes          : --live（場中）／--replay DATE [--codes ...]（保存5分足で再生）／--report（ランク別・ラベル別・スコア別の集計）

tachibana_live_flow.py      ← 最小の追記: 毎分の断面を live_flow/minutes/YYYY-MM-DD.jsonl に追記（1行=1分・約2,500銘柄・1日≈150MB→gzipで≈20MB）
                                 ＋ payload に "fibo": fibo_daytrade が書く fibo_live.json をそのまま同梱（Worker/WS改造不要）
kabuai/web/js/live.js       ← 🔥場中ライブに「📐 フィボ候補」セクション（#fibo-live）。📋朝の作戦の「作戦のいま」にも同じ行
.github / .env              ← Secret 1本: DISCORD_WEBHOOK_FIBO_URL（本人作業）
Windows タスク FiboDaytrade  ← 平日 08:58 起動・15:05 終了（LiveFlowと同じ流儀・pythonw）
```

**別プロセスにする理由**: LiveFlowは配信の生命線なので、フィボの計算やDiscord送信で落とさない。フィボ側はLiveFlowが書く毎分ファイルを読むだけ（読み専用・1分遅れ）。候補が出た銘柄だけ自分で `market_price()` を10秒間隔で取りに行く。

---

## 3. ロジックの置き方（指示書→実装の対応）

| 指示書 | 実装 | 補足・要確認 |
|---|---|---|
| 起点(100%) 通常=寄り後最安値／寄り陰線=寄り後最安値／GU=前日終値 | `origin()`：09:00〜高値確定までの `pDLP`（GUなら`pPRP`） | GUの閾値（何%からGU扱いか）が未定義 → **案: 寄り≥前日終値+1%**。+10%超は除外 |
| 初動ランクA/B/C/警戒/見送り | 起点→当日高値の上昇率と所要分・出来高3倍 | 出来高3倍の基準は「前日同時間帯」（毎分保存が貯まるまでは20日平均進捗比） |
| 高値確定 | 高値足の次の5分足が高値未更新かつ陰線で確定 | 1分断面版は「5分足の確定」を毎分の締めで判定 |
| フィボ 23.6〜161.8 | `fib_levels(origin, high)` | 高値更新で引き直し・起点割れで破棄・その日最初の波だけ・11:30以降の初波は対象外 |
| エントリー5条件 | `entry_check()`：38.2〜61.8停止／押し中出来高<上昇中／MA25上向き(直近5〜6本の傾き)かつ価格上／MA5上抜け足の確定／RCI(9本)が-80以下→上向き | 9時台はMA15、10時以降はMA25を「支え」に。寄り陰線日は「寄り足高値上抜け済み」フラグ |
| 重なりスコア | `overlap_score()`：±0.5%内のVWAP/中期MA(上向き)/前日高値/前日終値/価格帯出来高の山/キリ番/寄り足高値 → 3点以上=本命 | キリ番=100円単位（株価500円未満は50円単位にする案） |
| 決済 | `exit_plan()`：損切り=78.6%割れ or -3%の早い方（-3%より遠ければ不可）／+3%で半分→建値ストップ→+5%か127.2%の近い方／初動+4〜6%は直近高値手前・RR<1.5見送り／30分無反応撤退／14:30新規なし・15:00全決済 | 通知に エントリー/損切り/利確/RR を数字で載せる |
| 見送り6条件 | `skip_reason()` | 全部CSVに理由を残す（後で「見送りが正しかったか」も集計） |
| 寄り天反発 | `label="寄り天反発"`：フィボ①寄り高値→当日安値、②当日安値→反発高値 | 条件と利確は指示書どおり。①の壁まで+3%未満は見送り |
| リスク管理 | `RiskState`：2連敗で当日停止・負け後30分クールダウン・HALF_LOT_MODE=最初の10トレード半ロット | 「トレード」は通知ベースの紙で数える（発注しないので勝敗は紙の決済ルールで判定） |

---

## 4. 通知・記録・アプリ配信

- **Discord**（`DISCORD_WEBHOOK_FIBO_URL`）: 銘柄／ラベル（通常・ギャップ・寄り陰線・寄り天反発）／初動ランク／フィボ各価格／重なりスコア／エントリー・損切り・利確・RR。見送りは送らない（CSVのみ）。
- **CSV** `fibo_daytrade_log.csv`: 日付・銘柄・ラベル・初動値幅・所要分・止まったフィボ・中期MAの向き・重なりスコア・エントリー/決済価格・保有時間・結果・見送り理由。`--report` でランク別/ラベル別/スコア別の勝率・PF。
- **アプリ（リアルタイム）**: LiveFlowのpayloadに `fibo` を同梱 → Cloudflare `/publish` → WebSocketで即push（Worker改造不要）。🔥場中ライブに「📐 フィボ候補」（起点・高値・止まったフィボ・スコア・エントリー/損切り/利確）。遅延は約1分（候補銘柄は10秒）。

---

## 5. 再生モードと受け入れテスト

- `python fibo_daytrade.py --replay 2026-09-16 --codes 6208,338A,9130` → 判定の時系列を表示（起点/ランク/高値確定/フィボ/エントリー or 見送り理由）。
- 受け入れ: 6208=見送り（寄り天・78.6%割れ）／338A=起点=前日終値・1回目の押し候補・2回目見送り／9130=61.8%停止→エントリー判定。**338Aは現データで起点割れになるので、最初にここを突き合わせる。9130は母集団追加が必要。**
- **採否の関門（実装後・通知を実弾に使う前）**: 59日×1,161銘柄で全判定を再生し、ランダム同時刻買い（-0.1%/件）とBT作法（前後半PF>1.15・n≥150・上3日除いてプラス）で判定。通らなければ「紙で観察」のまま。

---

## 6. 工程と規模

| 段階 | 内容 | 目安 |
|---|---|---|
| P0 | エンジン＋再生モード＋例示3件のテスト（yfinance 5分足） | 1日 |
| P1 | LiveFlowの毎分保存＋ライブモード＋Discord＋CSV＋リスク管理 | 1日 |
| P2 | アプリ配信（fibo同梱・live.js セクション・作戦のいま） | 半日 |
| P3 | 59日再生で採否判定 → 紙運用開始 | 半日＋観察 |

## 7. 確認したいこと（材料で答えが変わる所だけ）
1. GU扱いの閾値（案: 寄り≥前日終値+1%。+10%超は除外）
2. 出来高3倍の基準: 前日同時間帯（毎分保存が貯まるまでは20日平均進捗比で代用）でよいか
3. 対象母集団: LiveFlowの流動上位約2,500（代金1億以上）でよいか。土俵15本を優先表示するか
4. 338Aの起点（現データでは前日終値割れ＝破棄）。あなたの板でどう見えていたか
5. リスク管理の「トレード」は紙の決済ルールで数える（発注しないため）でよいか
